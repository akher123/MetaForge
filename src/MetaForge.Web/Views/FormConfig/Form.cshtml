@{
    ViewData["Title"] = ViewBag.IsEdit == true ? "Edit Form" : "New Form";
    var isEdit = ViewBag.IsEdit as bool? ?? false;
    var screen = ViewBag.Screen as MetaForge.Application.DTOs.FormBuilderScreenDto;
    var module = screen?.Master ?? ViewBag.Form as MetaForge.Application.DTOs.FormConfigDto;
    var entities = ViewBag.Entities as IReadOnlyList<MetaForge.Application.DTOs.DiscoveredEntityOptionDto>;
    var defaultGroup = Context.Request.Query["group"].FirstOrDefault() ?? "Master Data";
    var defaultScreenType = defaultGroup == "Transaction" ? "MasterDetail" : "Master";
}

<div id="formBuilderApp"
     class="form-builder-app module-page form-builder-page"
     data-is-edit="@isEdit.ToString().ToLower()"
     data-default-group="@defaultGroup"
     data-default-screen-type="@defaultScreenType">

    <div class="page-header module-page-header module-page-header--breadcrumb mb-4">
        <div class="module-grid-breadcrumb">
            @await Component.InvokeAsync("Breadcrumb", new { currentPage = isEdit ? "Edit" : "Create" })
        </div>
    </div>

    <div class="card admin-card mb-4">
        <div class="card-header"><i class="fa-solid fa-sliders me-2"></i>Screen Setup</div>
        <div class="card-body">
            <div class="form-builder-form-section">
                <div class="form-builder-form-section-title">
                    <i class="fa-solid fa-database"></i>Entity &amp; Screen Type
                </div>
                <div class="row g-3">
                    <div class="col-lg-5">
                        <label class="form-label" for="entitySelect">Database Entity *</label>
                        <select id="entitySelect" class="form-select" @(isEdit ? "disabled" : "")>
                            <option value="">— Select Entity —</option>
                            @if (entities != null)
                            {
                                @foreach (var e in entities)
                                {
                                    <option value="@e.EntityName" data-table="@e.TableName" data-configured="@e.IsConfigured.ToString().ToLower()">
                                        @e.EntityName (@e.TableName)@(e.IsConfigured ? " · configured" : "")
                                    </option>
                                }
                            }
                        </select>
                        <div class="form-builder-help-text">Pick the EF Core entity this screen will manage.</div>
                    </div>
                    <div class="col-lg-7">
                        <label class="form-label">Screen Type *</label>
                        <select id="screenType" class="d-none" aria-hidden="true">
                            <option value="Master">Master Form</option>
                            <option value="Tabbed">Tabbed Form</option>
                            <option value="MasterDetail">Master + Detail</option>
                            <option value="MasterDetailTabular">Master + Tabular Details</option>
                            <option value="TreeViewMultiTable">Multi-Table Tree</option>
                        </select>
                        <div class="form-builder-type-options" role="radiogroup" aria-label="Screen type">
                            <label class="form-builder-type-option @(defaultScreenType == "Master" ? "is-selected" : "")" data-type="Master">
                                <input type="radio" name="screenTypeOption" value="Master" @(defaultScreenType == "Master" ? "checked" : null) />
                                <span class="form-builder-type-option-icon type-master"><i class="fa-solid fa-table"></i></span>
                                <span class="form-builder-type-option-label">Master Form</span>
                                <span class="form-builder-type-option-desc">Single entity list and edit screen.</span>
                            </label>
                            <label class="form-builder-type-option" data-type="Tabbed">
                                <input type="radio" name="screenTypeOption" value="Tabbed" />
                                <span class="form-builder-type-option-icon type-tabbed"><i class="fa-solid fa-folder-tree"></i></span>
                                <span class="form-builder-type-option-label">Tabbed Form</span>
                                <span class="form-builder-type-option-desc">Single entity with fields grouped in tabs (ERP-style).</span>
                            </label>
                            <label class="form-builder-type-option @(defaultScreenType == "MasterDetail" ? "is-selected" : "")" data-type="MasterDetail">
                                <input type="radio" name="screenTypeOption" value="MasterDetail" @(defaultScreenType == "MasterDetail" ? "checked" : null) />
                                <span class="form-builder-type-option-icon type-detail"><i class="fa-solid fa-diagram-project"></i></span>
                                <span class="form-builder-type-option-label">Master + Detail</span>
                                <span class="form-builder-type-option-desc">Header form with one detail line section.</span>
                            </label>
                            <label class="form-builder-type-option" data-type="MasterDetailTabular">
                                <input type="radio" name="screenTypeOption" value="MasterDetailTabular" />
                                <span class="form-builder-type-option-icon type-tabular"><i class="fa-solid fa-table-list"></i></span>
                                <span class="form-builder-type-option-label">Master + Tabular</span>
                                <span class="form-builder-type-option-desc">Header with multiple tabbed child grids.</span>
                            </label>
                            <label class="form-builder-type-option" data-type="TreeViewMultiTable">
                                <input type="radio" name="screenTypeOption" value="TreeViewMultiTable" />
                                <span class="form-builder-type-option-icon type-tree"><i class="fa-solid fa-sitemap"></i></span>
                                <span class="form-builder-type-option-label">Multi-Table Tree</span>
                                <span class="form-builder-type-option-desc">Hierarchical tree across related entities with paging and search.</span>
                            </label>
                        </div>
                    </div>
                </div>
            </div>

            <div class="form-builder-form-section">
                <div class="form-builder-form-section-title">
                    <i class="fa-solid fa-tag"></i>Identity
                </div>
                <div class="row g-3">
                    <div class="col-md-3">
                        <label class="form-label" for="groupName">Group *</label>
                        <select id="groupName" class="form-select">
                            <option value="Master Data">Master Data</option>
                            <option value="Transaction">Transaction</option>
                        </select>
                    </div>
                    <div class="col-md-3">
                        <label class="form-label" for="formCode">Code *</label>
                        <input type="text" id="formCode" class="form-control" placeholder="salesorder" />
                    </div>
                    <div class="col-md-6">
                        <label class="form-label" for="moduleName">Display Name *</label>
                        <input type="text" id="moduleName" class="form-control" placeholder="Sales Order" />
                    </div>
                </div>
            </div>

            <div class="form-builder-form-section mb-0">
                <div class="form-builder-form-section-title">
                    <i class="fa-solid fa-gear"></i>Options
                </div>
                <div class="row g-3 align-items-end">
                    <div class="col-md-3">
                        <label class="form-label" for="entityName">Entity Name</label>
                        <input type="text" id="entityName" class="form-control" readonly />
                    </div>
                    <div class="col-md-3">
                        <label class="form-label" for="tableName">Table Name</label>
                        <input type="text" id="tableName" class="form-control" readonly />
                    </div>
                    <div class="col-md-2">
                        <label class="form-label" for="displayOrder">Display Order</label>
                        <input type="number" id="displayOrder" class="form-control" value="0" min="0" />
                    </div>
                    <div class="col-md-2">
                        <div class="form-check form-switch mb-2">
                            <input type="checkbox" id="isActive" class="form-check-input" role="switch" checked />
                            <label class="form-check-label" for="isActive">Active</label>
                        </div>
                    </div>
                    <div class="col-md-2">
                        @if (!isEdit)
                        {
                            <button type="button" id="btnLoadDraft" class="btn btn-outline-primary w-100">
                                <i class="fa-solid fa-wand-magic-sparkles me-1"></i>Auto-Build
                            </button>
                        }
                        else
                        {
                            <button type="button" id="btnSyncFromEntity" class="btn btn-outline-info w-100" title="Merge new entity properties into this form">
                                <i class="fa-solid fa-rotate me-1"></i>Sync from Entity
                            </button>
                        }
                    </div>
                </div>
            </div>

            <div id="detailEntityInfo" class="form-builder-info-banner mt-3 d-none">
                <i class="fa-solid fa-diagram-project me-2"></i>
                Detail entity: <strong id="detailEntityName"></strong>
                (<span id="detailForeignKey"></span>)
            </div>
        </div>
    </div>

    <div class="card admin-card form-builder-workspace-card mb-4">
        <ul class="nav nav-tabs form-builder-tabs px-2 pt-2" role="tablist">
            <li class="nav-item" role="presentation">
                <button class="nav-link active" id="tab-master-btn" data-bs-toggle="tab" data-bs-target="#tabMaster" type="button" role="tab">
                    <i class="fa-solid fa-file-lines me-1"></i> Master Form
                </button>
            </li>
            <li class="nav-item d-none" id="tab-detail-nav" role="presentation">
                <button class="nav-link" id="tab-detail-btn" data-bs-toggle="tab" data-bs-target="#tabDetail" type="button" role="tab">
                    <i class="fa-solid fa-table-list me-1"></i> Detail Form
                </button>
            </li>
            <li class="nav-item" role="presentation">
                <button class="nav-link" id="tab-grid-btn" data-bs-toggle="tab" data-bs-target="#tabGrid" type="button" role="tab">
                    <i class="fa-solid fa-table me-1"></i> List Grid
                </button>
            </li>
            <li class="nav-item d-none" id="tab-tree-levels-nav" role="presentation">
                <button class="nav-link" id="tab-tree-levels-btn" data-bs-toggle="tab" data-bs-target="#tabTreeLevels" type="button" role="tab">
                    <i class="fa-solid fa-sitemap me-1"></i> Tree Levels
                </button>
            </li>
            <li class="nav-item" role="presentation">
                <button class="nav-link" id="tab-relations-btn" data-bs-toggle="tab" data-bs-target="#tabRelations" type="button" role="tab">
                    <i class="fa-solid fa-link me-1"></i> Relations
                </button>
            </li>
        </ul>

        <div class="tab-content form-builder-tab-content">
            <div class="tab-pane fade show active" id="tabMaster" role="tabpanel">
                <div class="form-builder-workspace">
                    <div class="form-builder-panel form-builder-fields-panel">
                        <div class="form-builder-panel-header">
                            <span class="fw-semibold"><i class="fa-solid fa-list me-1"></i> Master Fields</span>
                            <button type="button" id="btnAddMasterField" class="btn btn-sm btn-outline-primary">
                                <i class="fa-solid fa-plus me-1"></i>Add Field
                            </button>
                        </div>
                        <div id="tabbedSectionHint" class="form-builder-info-banner mx-0 mb-0 rounded-0 border-start-0 border-end-0 border-top-0 d-none">
                            <i class="fa-solid fa-folder-tree me-2 text-primary"></i>
                            <strong>Tabbed form convention:</strong> use the <strong>Section</strong> column to assign each field to a tab.
                            Recommended section names: <code>General</code>, <code>Contacts</code>, <code>Location</code>, <code>Accounting</code>.
                            Leave section empty to place fields under <strong>General</strong>. Tab order follows the lowest field display order in each section.
                        </div>
                        <div class="validation-rule-hint alert alert-light border py-2 px-3 mx-0 mb-0 rounded-0 border-start-0 border-end-0">
                            <i class="fa-solid fa-shield-halved me-2 text-primary"></i>
                            <strong>Validation rules:</strong> scroll right in the field table if needed, then click the blue <strong>Rules</strong> button in the <strong>Validation</strong> column.
                            Set length, range, email, regex, or cross-field compare rules. Click <strong>Save Form</strong> when done.
                        </div>
                        <div class="table-responsive">
                            <table class="table table-sm mb-0 form-builder-field-table" id="masterFieldsTable">
                                <thead>
                                    <tr>
                                        <th class="col-order"></th>
                                        <th>Property</th>
                                        <th>Label</th>
                                        <th>Control</th>
                                        <th>Section</th>
                                        <th>Lookup</th>
                                        <th>Display Field</th>
                                        <th>Value Field</th>
                                        <th>Cascade From</th>
                                        <th>Filter Field</th>
                                        <th>Mapping Entity</th>
                                        <th>Mapping Parent Key</th>
                                        <th>Mapping Related Key</th>
                                        <th title="Required">Req</th>
                                        <th title="Visible">Vis</th>
                                        <th title="Read Only">RO</th>
                                        <th class="field-validation-col" title="Click Rules to open the validation rule builder">Validation <i class="fa-solid fa-shield-halved text-primary"></i></th>
                                        <th class="field-conditional-col">
                                            Conditional
                                            <button type="button" class="btn btn-link btn-sm p-0 ms-1 align-baseline conditional-col-help" aria-label="Conditional rules reference">
                                                <i class="fa-solid fa-circle-info text-info"></i>
                                            </button>
                                        </th>
                                        <th></th>
                                    </tr>
                                </thead>
                                <tbody></tbody>
                            </table>
                        </div>
                    </div>
                    <div class="form-builder-panel form-builder-preview-panel">
                        <div class="form-builder-panel-header">
                            <span class="fw-semibold"><i class="fa-solid fa-eye me-1"></i> Live Preview</span>
                        </div>
                        <div class="form-builder-preview-body">
                            <div id="masterFormPreview" class="admin-form admin-form-preview"></div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="tab-pane fade" id="tabDetail" role="tabpanel">
                <div class="form-builder-workspace">
                    <div class="form-builder-panel form-builder-fields-panel">
                        <div class="form-builder-panel-header">
                            <span class="fw-semibold"><i class="fa-solid fa-list me-1"></i> Detail Line Fields</span>
                            <button type="button" id="btnAddDetailField" class="btn btn-sm btn-outline-primary">
                                <i class="fa-solid fa-plus me-1"></i>Add Field
                            </button>
                        </div>
                        <div class="validation-rule-hint alert alert-light border py-2 px-3 mx-0 mb-0 rounded-0 border-start-0 border-end-0">
                            <i class="fa-solid fa-shield-halved me-2 text-primary"></i>
                            Use the <strong>Rules</strong> button in the <strong>Validation</strong> column to configure detail line validation.
                        </div>
                        <div class="table-responsive">
                            <table class="table table-sm mb-0 form-builder-field-table" id="detailFieldsTable">
                                <thead>
                                    <tr>
                                        <th class="col-order"></th>
                                        <th>Property</th>
                                        <th>Label</th>
                                        <th>Control</th>
                                        <th>Section</th>
                                        <th>Lookup</th>
                                        <th>Display Field</th>
                                        <th>Value Field</th>
                                        <th>Cascade From</th>
                                        <th>Filter Field</th>
                                        <th>Mapping Entity</th>
                                        <th>Mapping Parent Key</th>
                                        <th>Mapping Related Key</th>
                                        <th title="Required">Req</th>
                                        <th title="Visible">Vis</th>
                                        <th title="Read Only">RO</th>
                                        <th class="field-validation-col" title="Click Rules to open the validation rule builder">Validation <i class="fa-solid fa-shield-halved text-primary"></i></th>
                                        <th class="field-conditional-col">
                                            Conditional
                                            <button type="button" class="btn btn-link btn-sm p-0 ms-1 align-baseline conditional-col-help" aria-label="Conditional rules reference">
                                                <i class="fa-solid fa-circle-info text-info"></i>
                                            </button>
                                        </th>
                                        <th></th>
                                    </tr>
                                </thead>
                                <tbody></tbody>
                            </table>
                        </div>
                    </div>
                    <div class="form-builder-panel form-builder-preview-panel">
                        <div class="form-builder-panel-header">
                            <span class="fw-semibold"><i class="fa-solid fa-eye me-1"></i> Live Preview</span>
                        </div>
                        <div class="form-builder-preview-body">
                            <div id="detailFormPreview" class="admin-form admin-form-preview admin-form-inline"></div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="tab-pane fade" id="tabGrid" role="tabpanel">
                <div class="form-builder-panel-header">
                    <span class="fw-semibold"><i class="fa-solid fa-table me-1"></i> List Grid Columns</span>
                    <small class="text-muted ms-2">Date columns use user/system localization preferences.</small>
                    <button type="button" id="btnAddColumn" class="btn btn-sm btn-outline-primary">
                        <i class="fa-solid fa-plus me-1"></i>Add Column
                    </button>
                </div>
                <div class="table-responsive px-3 pb-3">
                    <table class="table table-sm table-striped mb-0 form-builder-field-table" id="columnsTable">
                        <thead>
                            <tr>
                                <th>Property</th>
                                <th>Label</th>
                                <th>Sortable</th>
                                <th>Searchable</th>
                                <th>Visible</th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody></tbody>
                    </table>
                </div>

                <div class="form-builder-panel-header border-top">
                    <span class="fw-semibold"><i class="fa-solid fa-bolt me-1"></i> Custom Grid Actions</span>
                    <button type="button" id="btnAddGridAction" class="btn btn-sm btn-outline-primary">
                        <i class="fa-solid fa-plus me-1"></i>Add Action
                    </button>
                </div>
                <div class="table-responsive px-3 pb-3">
                    <table class="table table-sm table-striped mb-0 form-builder-field-table" id="gridActionsTable">
                        <thead>
                            <tr>
                                <th>Code</th>
                                <th>Label</th>
                                <th>Icon</th>
                                <th>Placement</th>
                                <th>Handler</th>
                                <th>Target</th>
                                <th>Method</th>
                                <th>Body</th>
                                <th>Permission</th>
                                <th>Confirm</th>
                                <th>Style</th>
                                <th title="Active">On</th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody></tbody>
                    </table>
                </div>
                <div class="form-builder-help-card mx-3 mb-3">
                    <strong>Toolbar</strong> actions appear above the grid (e.g. bulk export, sync).
                    <strong>Row</strong> actions appear on each line (e.g. Approve, Print).
                    Use placeholders <code>{id}</code>, <code>{formCode}</code>, <code>{entity}</code>, and any row property name in targets and JSON bodies.
                    <strong>Script</strong> handlers call <code>MetaForgeGridActionHandlers[code](context)</code> registered in custom JavaScript.
                </div>
            </div>

            <div class="tab-pane fade" id="tabTreeLevels" role="tabpanel">
                <div class="form-builder-panel-header">
                    <span class="fw-semibold"><i class="fa-solid fa-sitemap me-1"></i> Tree Level Chain</span>
                    <button type="button" id="btnAddTreeLevel" class="btn btn-sm btn-outline-primary">
                        <i class="fa-solid fa-plus me-1"></i>Add Level
                    </button>
                </div>
                <div class="form-builder-info-banner mx-0 mb-0 rounded-0 border-start-0 border-end-0 border-top-0">
                    <i class="fa-solid fa-circle-info me-2 text-primary"></i>
                    Level 0 is the <strong>root entity</strong> (synced from Entity &amp; Screen Type).
                    Each child level must link to the previous entity via a foreign key (e.g. Country → Region via <code>CountryId</code>).
                    Configure root fields on <strong>Master Form</strong> and <strong>List Grid</strong> tabs.
                    Child entity forms are auto-created when saved (or reuse existing entity forms).
                </div>
                <div class="table-responsive px-3 pb-3">
                    <table class="table table-sm table-striped mb-0 form-builder-field-table" id="treeLevelsTable">
                        <thead>
                            <tr>
                                <th>Level</th>
                                <th>Entity</th>
                                <th>Parent Entity</th>
                                <th>Foreign Key</th>
                                <th title="Comma-separated property names shown in the tree grid for this level">Display Columns</th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody></tbody>
                    </table>
                </div>
            </div>

            <div class="tab-pane fade" id="tabRelations" role="tabpanel">
                <div class="form-builder-panel-header">
                    <span class="fw-semibold"><i class="fa-solid fa-link me-1"></i> Entity Relations</span>
                    <button type="button" id="btnAddRelation" class="btn btn-sm btn-outline-primary">
                        <i class="fa-solid fa-plus me-1"></i>Add Relation
                    </button>
                </div>
                <div class="table-responsive px-3 pb-2">
                    <table class="table table-sm table-striped mb-0 form-builder-field-table" id="relationsTable">
                        <thead>
                            <tr>
                                <th>Type</th>
                                <th>Parent Entity</th>
                                <th>Child Entity</th>
                                <th>Foreign Key</th>
                                <th>Navigation</th>
                                <th>Tab Label</th>
                                <th>Order</th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody></tbody>
                    </table>
                </div>
                <div class="form-builder-help-card mx-3 mb-3">
                    <strong>Tip:</strong> For <em>Master + Detail</em>, add one <strong>OneToMany</strong> relation (e.g. SalesOrder → SalesOrderItem).
                    For <em>Master + Tabular</em>, add multiple <strong>OneToMany</strong> relations — each becomes a tab with its own child grid.
                    Use the <strong>Detail Form</strong> tab for the primary child; other child forms are auto-created from entity metadata when saved.
                </div>
            </div>
        </div>
    </div>

    <div class="form-builder-help-card mb-3">
        <i class="fa-solid fa-lightbulb me-2 text-warning"></i>
        For dropdown or autocomplete fields, set <strong>Lookup</strong> to the entity name.
        Set <strong>Display Field</strong> to the lookup property shown in the list, or combine fields using a template such as <code>{Code} - {Name}</code> or comma-separated values such as <code>Code, Name</code>.
        Set <strong>Value Field</strong> to the property stored in the form (usually <code>Id</code>).
        Use <strong>Cascade From</strong> to filter child lookups by a parent field (e.g. Region by CountryId). Dropdown and Autocomplete store a single FK on the entity; MultiSelect uses autocomplete search with checkboxes and the mapping columns to persist selected ids in a junction table.
        Use <strong>Autocomplete</strong> for large lookup lists — loads 10 items at a time and fetches more as you type or scroll.
        Optional <strong>Filter Field</strong> overrides the lookup property name.
        Click the <i class="fa-solid fa-shield-halved"></i> button in the <strong>Validation</strong> column to configure rules visually (length, range, email, regex, cross-field compare).
        Use the <i class="fa-solid fa-code-branch"></i> button in the <strong>Conditional</strong> column to show, hide, enable, disable, or require fields based on other field values.
        Hover the summary or the <i class="fa-solid fa-circle-info"></i> icon in the column header to see configured rules and the actions/operators reference.
        In edit mode, use <strong>Sync from Entity</strong> to merge new EF Core properties into the form while keeping existing validation and conditional rules.
    </div>

    <div class="form-builder-save-bar d-flex flex-wrap gap-2 mb-5">
        <button type="button" id="btnSaveScreen" class="btn btn-primary">
            <i class="fa-solid fa-floppy-disk me-1"></i>Save Form
        </button>
        <a href="/FormBuilder" class="btn btn-outline-secondary">
            <i class="fa-solid fa-xmark me-1"></i>Cancel
        </a>
    </div>
</div>

@section Scripts {
    <script>
        window.__formBuilderData = {
            screen: @Html.Raw(screen != null ? System.Text.Json.JsonSerializer.Serialize(screen) : "null"),
            module: @Html.Raw(module != null && screen == null ? System.Text.Json.JsonSerializer.Serialize(module) : "null"),
            entities: @Html.Raw(System.Text.Json.JsonSerializer.Serialize(entities))
        };
    </script>
    <script src="~/js/metaforge/lookup-cascade.js" asp-append-version="true"></script>
    <script src="~/js/metaforge/field-conditional-engine.js" asp-append-version="true"></script>
    <script src="~/js/metaforge/dynamic-form.js" asp-append-version="true"></script>
    <script src="~/js/metaforge/validation-rule-builder.js" asp-append-version="true"></script>
    <script src="~/js/metaforge/conditional-rule-builder.js" asp-append-version="true"></script>
    <script src="~/js/metaforge/entity-schema-sync.js" asp-append-version="true"></script>
    <script src="~/js/metaforge/module-config.js" asp-append-version="true"></script>
}
